service: serverless-aws-lambda

frameworkVersion: '3'

plugins:
  - serverless-plugin-typescript

custom:
  serverless-plugin-typescript:
    tsConfigFileLocation: './tsconfig.json'

provider:
  name: aws
  runtime: nodejs18.x
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  memorySize: 512
  timeout: 30

  # Use existing LabRole for AWS Academy
  iam:
    role: ${env:AWS_ROLE_ARN}

  environment:
    STAGE: ${self:provider.stage}
    REGION: ${self:provider.region}
    # RDS PostgreSQL connection (from secrets)
    DB_HOST: ${env:DB_HOST}
    DB_PORT: ${env:DB_PORT}
    DB_NAME: ${env:DB_NAME}
    DB_USER: ${env:DB_USER}
    DB_PASSWORD: ${env:DB_PASSWORD}

functions:
  getCustomer:
    handler: src/handler.getCustomer
    description: 'Get customer by CPF from PostgreSQL RDS'
    events:
      - http:
          path: /customers
          method: get
          cors: true
          request:
            parameters:
              querystrings:
                cpf: true
